package com.smallworld;

//java -cp target/classes com.smallworld.TransactionApplication
public class TransactionApplication {
    public static void main(String[] args) {
        //Read JSON file
        //Map JSON to Java objects
        //During mapping make sure if a transaction is repeated due to multiple issues
        //the issues are added in the existing transaction object instead of creating a new one
    }
}
